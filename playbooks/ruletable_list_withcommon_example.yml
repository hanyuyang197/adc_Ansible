---
- name: List Rule Tables with Common Partition
  hosts: adc_servers
  gather_facts: no
  vars:
    # # adc_username: "{{ adc_username }}"  # 循环引用已修复  # 循环引用已修复
    # # adc_password: "{{ adc_password }}"  # 循环引用已修复  # 循环引用已修复
  tasks:
    - name: 登录ADC设备
      adc_login:
        ip: "{{ inventory_hostname }}"
        username: "{{ adc_username }}"
        password: "{{ adc_password }}"
      register: login_result

    - name: Get list of rule tables including common partition
      adc_slb_ruletable:
        ip: "{{ inventory_hostname }}"
        authkey: "{{ login_result.authkey }}"
        action: list_ruletables_withcommon
      register: result

    - name: Display rule tables
      debug:
        var: result.result

    - name: 登出ADC设备
      adc_logout:
        ip: "{{ inventory_hostname }}"
        authkey: "{{ login_result.authkey }}"
